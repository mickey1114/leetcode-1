class Solution {
public:
    bool isPalindrome(int x) {
    //題目給的是整數 我把整數先轉成字串
        string l=to_string(x);
        //以下我將字串除以2 分成左右兩邊判斷
        for(int i=0;i<l.size()/2;i++){
            if(l[i]!=l[l.size()-i-1])
                return false;
        }
        return true;
    }
};
